#
# GENERATED BY docker/init.sh - template: ./templates/docker-compose.yml - 9b9696447bd387ebfed048344d4c20bc
#
# ======================================================================
#
# (1) see .env for set variables
# (2) run "docker-compose up" to startup
# 
# ======================================================================
version: '3.9'

networks:
  appmonitor-network:

services:

  # ----- apache httpd + php
  appmonitor-web:
    build:
      context: .
      dockerfile: ./containers/web-server/Dockerfile
    # image: "php:8.1-apache"
    container_name: 'appmonitor-server'
    ports:
      - '${APP_PORT}:80'

    working_dir: ${WEBROOT}
    
    volumes:
      - ../:/var/www/${APP_NAME}
      - ./containers/web-server/apache/sites-enabled:/etc/apache2/sites-enabled
      - ./containers/web-server/php/extra-php-config.ini:/usr/local/etc/php/conf.d/extra-php-config.ini
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 10s
      timeout: 3s
      retries: 5
      # start_period: 40s

    networks:
      - appmonitor-network

    user: ${DOCKER_USER_UID}

